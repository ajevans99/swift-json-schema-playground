// Build-time constant injected by Vite's `define` (see `vite.config.ts`).
// The value is the `state.version` of the `swift-json-schema` pin in
// `wasm/Package.resolved`, or the literal string `"unknown"` if the file
// could not be read.
declare const __SWIFT_JSON_SCHEMA_VERSION__: string
